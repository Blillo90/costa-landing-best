{
  "name": "costa-landing-best",
  "type": "module",
  "version": "0.0.1",
  "scripts": {
    "dev": "astro dev",
    "build": "astro build",
    "preview": "astro preview",
    "astro": "astro",

    "db:up": "docker compose up -d",
    "db:down": "docker compose down",
    "db:reset": "docker compose down -v && docker compose up -d",
    "db:schema": "docker compose exec -T postgres psql -U astro -d leads_db < db/schema.sql"
  },
  "dependencies": {
    "@astrojs/vercel": "^9.0.2",
    "@tailwindcss/vite": "^4.1.18",
    "@vercel/postgres": "^0.10.0",
    "astro": "^5.16.6",
    "tailwindcss": "^4.1.18",
    "zod": "^3.25.76"
  }
}
