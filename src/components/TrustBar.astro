---
import type { Lang } from "../content/i18n";
import { copy } from "../content/i18n";
const { lang } = Astro.props as { lang: Lang };
const t = copy[lang];
---

<section class="border-y border-white/10 bg-black">
  <div class="mx-auto max-w-6xl px-6 py-8 grid grid-cols-2 md:grid-cols-4 gap-6 text-sm text-white/80">
    <div>⭐ {t.trust.rating}</div>
    <div>🏆 {t.trust.years}</div>
    <div>🤝 {t.trust.deals}</div>
    <div>📍 {t.trust.office}</div>
  </div>
</section>
